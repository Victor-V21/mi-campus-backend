meta {
  name: PaginationList
  type: http
  seq: 4
}

get {
  url: {{PATH}}/users
  body: none
  auth: bearer
}

params:query {
  ~searchTerm: 
  ~page: 1
  ~pageSize: 10
}

auth:bearer {
  token: {{TOKEN}}
}
